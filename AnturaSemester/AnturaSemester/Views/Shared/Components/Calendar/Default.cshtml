@model List<AnturaSemester.Models.Users>

@{
    ViewData["Title"] = "Calendar";
}

<nav aria-label="Page navigation">
    <ul class="pagination">
        <li class="active">
        <li>
            <a href="#" aria-label="Previous">
                <span aria-hidden="true">&laquo;</span>
            </a>
        </li>
        <li><a href="#">@ViewBag.currentMonth</></a></li>
        <li>
            <a href="#" aria-label="Next">
                <span aria-hidden="true">&raquo;</span>
            </a>
        </li>
    </ul>
</nav>


<table class=" table table-bordered table-hover">
    <tbody>
        <tr>
            @foreach (var x in ViewBag.Column)
            {

            }
            <th>

                @foreach (var x in ViewBag.Column) // Only numbers are highlighted red
                {
                    if (x.weekDay == true)
                    {
                    <td bgcolor="#e2dede" width="32px"><font color="red">@x.Day</font></td>
                    }
                    else
                    {
                        <td width="32px">@x.Day</td>
                    }
                }

                @foreach (var user in Model)
                {
                <tr>
                    <td>
                        @Html.DisplayFor(model => user.FullName)
                    </td>

                    @foreach (var x in ViewBag.Column)
                    {
                        
                        if (x.weekDay == true)
                        {
                            <td bgcolor="#e2dede"></td>
                        }
                        //else if user id and date of column (x.day, x.month, x.year) is in calendarcell database table
                        else
                        {
                            <td></td>
                        }

                    }

                    </tr>
                }





            </tbody>

        </table>




        <!-- hghgh -->
